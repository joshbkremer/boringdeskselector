[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA redirect — send all routes to index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
